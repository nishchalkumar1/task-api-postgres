[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

# PostgreSQL DSN — overridden at runtime by env var
sqlalchemy.url = postgresql+psycopg://postgres:postgres@db:5432/tasks
